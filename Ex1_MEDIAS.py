def Media(x,y,z):
    media = (x+y+z)/3
    return media

a = float(raw_input('Digite a primeira nota: '))
b = float(raw_input('Digite a segunta nota: '))
c = float(raw_input('Digite a terceira nota: '))
print Media(a,b,c)

# Nota: 1.0
# Comentario: Excelente Organizacao
